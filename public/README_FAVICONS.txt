Place your favicon assets here:
- favicon.ico (multi-size ICO generated from SVG)
- favicon.png (32x32 PNG generated from SVG)
- favicon.svg (source)

To (re)generate PNG and ICO from the SVG, run:

  npm run favicons

These are referenced by index.html via multiple <link rel> tags for maximum browser compatibility.
